# Proof artifacts: machine-generated, large, and rewritten wholesale on every
# reproof. Keep them out of diffs and out of GitHub's language stats and PR
# review surface. Use `git diff --text -- sessions/` to override.
sessions/** -diff linguist-generated=true
